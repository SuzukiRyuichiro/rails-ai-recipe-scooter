<%= form_with(model: reccipe) do |form| %>
  <% if reccipe.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reccipe.errors.count, "error") %> prohibited this reccipe from being saved:</h2>

      <ul>
        <% reccipe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :ingredients, style: "display: block" %>
    <%= form.text_field :ingredients %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_field :content %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
